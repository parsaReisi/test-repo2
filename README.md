# Demo 2

some text...

to create a git repository localy:

1. new folder
2. git init
3. to create the connection of your local git repository and your github profile, create a new repository in the site
4. git remote add origin "the ssh(or https?) link in quick sestup (after you created the repository)"
5. git push origin master